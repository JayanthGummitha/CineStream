/**
 * Debug Video Player Page
 * 
 * A test page to verify Netflix-like video features are working correctly
 */

import { VideoPlayerDebug } from '@/components/VideoPlayerDebug';

export default function DebugVideoPage() {
    return (
        <div className="w-full h-screen">
            <VideoPlayerDebug />
        </div>
    );
}